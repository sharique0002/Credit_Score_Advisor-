import React from "react";
import ReactDOM from "react-dom/client";
import CreditScoreAdvisor from "./score.jsx"; // Ensure this is correct
 // Change this if your CSS file has a different name

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <CreditScoreAdvisor />
  </React.StrictMode>
);
